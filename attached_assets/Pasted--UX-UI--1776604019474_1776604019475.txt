Проведи полный рефакторинг моего мессенджера без слома текущего UX/UI и без потери существующих данных. Критически важно: при всех изменениях, обновлениях, миграциях и рефакторинге не удаляй и не обнуляй существующие сообщения, чаты, профили, участников, invite-коды и любую текущую информацию; сохрани все данные, добавляй только безопасные миграции, делай backfill и совместимость со старой структурой, не используй destructive changes, reset базы, drop таблиц или очистку storage. Исправь главное: жёстко раздели роли admin и user на клиенте, сервере, маршрутах и API, чтобы админ-функции, админ-панель, кнопки, вкладки и маршруты были доступны только мне и не были видны обычным пользователям; убери старую клиентскую auth-логику как источник истины, полностью исключи обход через localStorage, demo-вход, старый setup-flow и любые дублирующиеся механики авторизации; сделай реальные одноразовые invite-коды с серверной проверкой, глобальной одноразовостью, отзывом, сроком действия и корректной ошибкой при повторном использовании; подключи и проверь все backend-роуты, роли, сессии, cookies, CORS, guards и права доступа; переведи профили, роли, чаты, участников, сообщения, invite-коды и админ-действия на реальное серверное хранение, убери все моковые данные, фальшивые статусы и декоративную имитацию активности, если они не настоящие; сделай настоящий realtime вместо медленного polling, либо максимально сократи задержки и внедри optimistic UI, чтобы текст, фото, голосовые и файлы появлялись сразу и синхронизировались корректно; исправь медленную отправку сообщений, фото и голосовых: добавь отдельный устойчивый upload flow, progress, preview, retry, статусы sending/sent/delivered/read/error, идемпотентную отправку через clientMessageId, защиту от дублей, повторных нажатий и race conditions; нормализуй медиа до загрузки, сжимай изображения, ограничь размер и формат файлов, продумай полноценный lifecycle голосовых: запись, предпрослушивание, отмена, отправка, ошибка; добавь пагинацию истории чатов, черновики по каждому чату, offline/reconnect/degraded mode, очередь неотправленных сообщений, централизованное логирование клиентских и серверных ошибок, аудит действий админа и типизированную обработку ошибок от backend до UI; сохрани визуальный стиль, текущую структуру экранов и общий UX максимально близкими к текущей версии, но убери всё, что делает приложение прототипом, нестабильным или небезопасным.